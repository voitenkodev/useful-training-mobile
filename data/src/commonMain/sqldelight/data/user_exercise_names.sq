CREATE TABLE UserExerciseNameEntity (
  value TEXT NOT NULL,
  PRIMARY KEY (value)
);

selectAll:
SELECT *
FROM UserExerciseNameEntity;

insert:
INSERT OR REPLACE INTO UserExerciseNameEntity(value)
VALUES (?);

delete:
DELETE FROM UserExerciseNameEntity
WHERE (value) = ?;