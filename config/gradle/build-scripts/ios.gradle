/*
    use with custom:
    cocoapods { project.file("../iosApp/Podfile") }
*/

kotlin.cocoapods {
    version = "1.0"
    summary = "Shared Code"
    homepage = "https://github.com/voitenkodev/Useful-Training"
    ios.deploymentTarget = "14.1"
    framework {
        baseName = "shared"
        isStatic = true
    }
    extraSpecAttributes["resources"] = "['src/commonMain/resources/**', 'src/iosMain/resources/**']"
}